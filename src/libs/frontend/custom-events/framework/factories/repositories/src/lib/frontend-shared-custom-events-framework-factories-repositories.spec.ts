import { frontendSharedCustomEventsFrameworkFactoriesRepositories } from './frontend-shared-custom-events-framework-factories-repositories';

describe('frontendSharedCustomEventsFrameworkFactoriesRepositories', () => {
  it('should work', () => {
    expect(frontendSharedCustomEventsFrameworkFactoriesRepositories()).toEqual(
      'frontend-shared-custom-events-framework-factories-repositories'
    );
  });
});
