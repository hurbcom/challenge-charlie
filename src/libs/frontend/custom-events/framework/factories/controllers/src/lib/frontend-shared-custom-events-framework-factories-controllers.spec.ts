import { frontendSharedCustomEventsFrameworkFactoriesControllers } from './frontend-shared-custom-events-framework-factories-controllers';

describe('frontendSharedCustomEventsFrameworkFactoriesControllers', () => {
  it('should work', () => {
    expect(frontendSharedCustomEventsFrameworkFactoriesControllers()).toEqual(
      'frontend-shared-custom-events-framework-factories-controllers'
    );
  });
});
