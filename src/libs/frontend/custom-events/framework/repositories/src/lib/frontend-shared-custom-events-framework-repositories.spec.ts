import { frontendSharedCustomEventsFrameworkRepositories } from './frontend-shared-custom-events-framework-repositories';

describe('frontendSharedCustomEventsFrameworkRepositories', () => {
  it('should work', () => {
    expect(frontendSharedCustomEventsFrameworkRepositories()).toEqual(
      'frontend-shared-custom-events-framework-repositories'
    );
  });
});
