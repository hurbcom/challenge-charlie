import { frontendWeatherForecastFrameworkRepositoriesAdapters } from './frontend-weather-forecast-framework-repositories-adapters';

describe('frontendWeatherForecastFrameworkRepositoriesAdapters', () => {
  it('should work', () => {
    expect(frontendWeatherForecastFrameworkRepositoriesAdapters()).toEqual(
      'frontend-weather-forecast-framework-repositories-adapters'
    );
  });
});
