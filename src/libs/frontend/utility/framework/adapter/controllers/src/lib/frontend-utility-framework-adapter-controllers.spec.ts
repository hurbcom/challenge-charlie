import { frontendUtilityFrameworkAdapterControllers } from './frontend-utility-framework-adapter-controllers';

describe('frontendUtilityFrameworkAdapterControllers', () => {
  it('should work', () => {
    expect(frontendUtilityFrameworkAdapterControllers()).toEqual(
      'frontend-utility-framework-adapter-controllers'
    );
  });
});
