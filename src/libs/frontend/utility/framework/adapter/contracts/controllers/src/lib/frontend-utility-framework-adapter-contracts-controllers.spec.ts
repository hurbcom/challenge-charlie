import { frontendUtilityFrameworkAdapterContractsControllers } from './frontend-utility-framework-adapter-contracts-controllers';

describe('frontendUtilityFrameworkAdapterContractsControllers', () => {
  it('should work', () => {
    expect(frontendUtilityFrameworkAdapterContractsControllers()).toEqual(
      'frontend-utility-framework-adapter-contracts-controllers'
    );
  });
});
