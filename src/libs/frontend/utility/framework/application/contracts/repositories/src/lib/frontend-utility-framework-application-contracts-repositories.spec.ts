import { frontendUtilityFrameworkApplicationContractsRepositories } from './frontend-utility-framework-application-contracts-repositories';

describe('frontendUtilityFrameworkApplicationContractsRepositories', () => {
  it('should work', () => {
    expect(frontendUtilityFrameworkApplicationContractsRepositories()).toEqual(
      'frontend-utility-framework-application-contracts-repositories'
    );
  });
});
