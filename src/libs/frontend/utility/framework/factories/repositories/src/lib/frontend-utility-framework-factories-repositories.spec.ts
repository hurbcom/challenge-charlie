import { frontendUtilityFrameworkFactoriesRepositories } from './frontend-utility-framework-factories-repositories';

describe('frontendUtilityFrameworkFactoriesRepositories', () => {
  it('should work', () => {
    expect(frontendUtilityFrameworkFactoriesRepositories()).toEqual(
      'frontend-utility-framework-factories-repositories'
    );
  });
});
