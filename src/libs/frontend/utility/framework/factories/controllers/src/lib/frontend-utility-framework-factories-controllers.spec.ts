import { frontendUtilityFrameworkFactoriesControllers } from './frontend-utility-framework-factories-controllers';

describe('frontendUtilityFrameworkFactoriesControllers', () => {
  it('should work', () => {
    expect(frontendUtilityFrameworkFactoriesControllers()).toEqual(
      'frontend-utility-framework-factories-controllers'
    );
  });
});
