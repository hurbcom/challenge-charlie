import { styled } from "~/lib/stitches";

export const Container = styled("div", {
  display: "flex",
  alignItems: "center",

  background: "$gray200",
});

export const Input = styled("input", {});
