import React from 'react';
import './styles.scss';

function Loading() {
    return (
        <div id="loading" title="Carregando">
            <div id="spinner" />
        </div>
    );
}

export default Loading;
