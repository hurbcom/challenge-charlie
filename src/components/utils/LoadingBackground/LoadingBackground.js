import React from 'react';

import { CircularProgress } from '@material-ui/core';

export default () => (
  <div className="d-flex flex-center background" style={{ backgroundColor: 'rgba(255, 255, 255, 0.3)' }}>
    <CircularProgress />
  </div>
);
