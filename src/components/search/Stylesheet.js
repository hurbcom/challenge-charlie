import styled from "styled-components";

export const Input = styled.input`
    width: 80%;
    height: 5vh;
    border: none;
    border-radius: 70px;
    background-color: rgba(0, 0, 0, 0.1);
    padding: 0 20px;
    font-size: 1.4rem;

    &:focus {
        outline: none;
    }
`;