import React from 'react'
import { Typography } from '@material-ui/core'

function GreatTitle() {
    return (
        <Typography
            variant='h5'
            style={{
                color: '#fff'
            }}
        >Tem clima?</Typography>
    )
}

export default GreatTitle