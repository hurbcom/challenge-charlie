import styled from 'styled-components';

export const SContainer = styled.strong`
  color: #ffffff;
  font-size: 1.2rem;
  font-weight: 500;

  &:first-child {
    text-transform: uppercase;
    cursor: pointer;
    margin-bottom: 1.4rem;
  }
`;
