import { FullScreenLoader, LoaderWrapper } from "./styled";

const Loader = () => {
    return (
        <FullScreenLoader>
            <LoaderWrapper />
        </FullScreenLoader>
    );
};

export default Loader;
