import React from 'react';
import { LocationInput } from '../LocationInput';
import { Container } from './styles';

export const App = () => {
  return (
    <Container>
      <LocationInput />
    </Container>
  );
};
