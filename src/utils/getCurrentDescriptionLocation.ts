/* import { GetCurrentCityState } from "../service/GetCurrentCityState"

export const getDescriptionLocation = async (lat:number, lng:number) => { 
    return await GetCurrentCityState().Get(lat as number, lng as number)
  }  */

 export const i = {}