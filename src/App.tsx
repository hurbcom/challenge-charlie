import React, { Component } from 'react';

import './App.css';

const App: React.FC = () => {
    return (
        <div className="container">
            <h1>Weather App</h1>
        </div>
    );
};

export default App;
