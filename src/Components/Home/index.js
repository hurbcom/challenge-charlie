import React from "react";
import Background from "../Background";

const Home = (props) => {
    return (
        <React.Fragment>
            <Background></Background>
        </React.Fragment>
    );
};

export default Home;
