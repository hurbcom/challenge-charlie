import { styled } from '~/lib/stitches';

export const Container = styled('main', {});

export const Content = styled('div', {
  width: '100%',
  height: '100vh',
  maxWidth: '600px',
  margin: '0 auto'
});
