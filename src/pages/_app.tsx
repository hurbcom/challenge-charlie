import React, { ReactNode } from "react";

const App = (): ReactNode => {
    return (
        <html>
            <body>
                <div>
                    <h1>Hello, World!</h1>
                </div>
            </body>
        </html>
    );
};

export default App