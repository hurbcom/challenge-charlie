import { RecoilRoot } from "recoil";
import { Background } from "../common/components/Background";
import { GlobalStyle } from "../common/styles/global";

export const Home = () => {
  return (
    <RecoilRoot>
      <Background>sdhfkjdhs</Background>
      <GlobalStyle />
    </RecoilRoot>
  );
};
