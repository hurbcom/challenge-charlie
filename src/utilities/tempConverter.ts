export function celsiusToFahrenheit(temp: number) {
  return Math.round(temp * 1.8 + 32);
}

/* Unused but could be applied to add a third temperature scale

 export function celsiusToKelvin(temp: number) {
   return temp + 273;
 }
*/
