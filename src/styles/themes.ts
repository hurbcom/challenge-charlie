import { createTheme } from "@mui/material/styles";

export const feTheme = createTheme({
  palette: {
    primary: {
      main: "#0012B9",
      contrastText: "#fff",
    },
  },
});

export const mfTheme = createTheme({
  palette: {
    primary: {
      main: "#0089EC",
      contrastText: "#fff",
    },
  },
});

export const fTheme = createTheme({
  palette: {
    primary: {
      main: "#96E6F8",
      contrastText: "#000",
    },
  },
});

export const freTheme = createTheme({
  palette: {
    primary: {
      main: "#54FF7A",
      contrastText: "#000",
    },
  },
});

export const qTheme = createTheme({
  palette: {
    primary: {
      main: "#FFC42E",
      contrastText: "#000",
    },
  },
});

export const mqTheme = createTheme({
  palette: {
    primary: {
      main: "#FF3426",
      contrastText: "#000",
    },
  },
});

export const pbTheme = createTheme({
  palette: {
    primary: {
      main: "#c8c8c8",
      contrastText: "#000",
    },
  },
});
