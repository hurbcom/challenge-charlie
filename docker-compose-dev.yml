version: "3.7"
services:
    react-app:
        container_name: challenge-charlie
        build:
            context: .
            dockerfile: Dockerfile.dev
        ports:
            - "3000:3000"
        volumes:
            - ./src:/challenge-charlie/src
