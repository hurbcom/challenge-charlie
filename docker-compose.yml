services:
  weather-app:
    stdin_open: true
    tty: true
    container_name: WeatherApp
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./src:/build/src
    env_file:
      - ./.env