export interface ThemeProps {
    colors: {
        primary: string
        secondary: string
    }
    typography: {
        fontFamily: string
        sizes: {
            title: string
            body: string
        }
        colors: {
            primary: string
            secondary: string
        }
    }
}
